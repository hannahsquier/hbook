var posts = "<%=j( render 'posts', user: @user ) %>"
var $posts = $(posts)

var newPostForm = "<%= j(render 'new_post_form') %>"
var $newPostForm = $(newPostForm)

$rightDiv = $("div#right")
$rightDiv.empty();

$rightDiv.append($newPostForm).append($posts)