(function() {
  var newComment = "<%= j(render 'posts/comment', comment: @comment, post: @post) %>"
  var $newComment = $(newComment)
  var $postDiv = $('div[data-post-id="<%= @post.id %>"]').children().filter(".post-and-comments");
  $newComment.appendTo($postDiv).hide().slideDown();
})();
